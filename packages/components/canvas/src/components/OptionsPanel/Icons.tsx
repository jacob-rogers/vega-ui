import { createIcon } from '@gpn-prototypes/vega-icons';

import {
  IconCursorSizeM,
  IconGroupingSizeM,
  IconHandSizeM,
  IconNodeEndSizeM,
  IconNodesSizeM,
  IconNodeStartSizeM,
  IconNodeStepSizeM,
  IconOrderingSizeM,
} from './canvas-icons';

export const IconCursor = createIcon({
  m: IconCursorSizeM,
  s: IconCursorSizeM,
  xs: IconCursorSizeM,
  name: 'IconCursor',
});

export const IconGrouping = createIcon({
  m: IconGroupingSizeM,
  s: IconGroupingSizeM,
  xs: IconGroupingSizeM,
  name: 'IconGrouping',
});

export const IconHand = createIcon({
  m: IconHandSizeM,
  s: IconHandSizeM,
  xs: IconHandSizeM,
  name: 'IconHand',
});

export const IconNodeEnd = createIcon({
  m: IconNodeEndSizeM,
  xs: IconNodeEndSizeM,
  s: IconNodeEndSizeM,
  name: 'IconNodeEnd',
});

export const IconNodeStart = createIcon({
  m: IconNodeStartSizeM,
  xs: IconNodeStartSizeM,
  s: IconNodeStartSizeM,
  name: 'IconNodeStart',
});

export const IconNodes = createIcon({
  m: IconNodesSizeM,
  xs: IconNodesSizeM,
  s: IconNodesSizeM,
  name: 'IconNodes',
});

export const IconNodeStep = createIcon({
  m: IconNodeStepSizeM,
  xs: IconNodeStepSizeM,
  s: IconNodeStepSizeM,
  name: 'IconNodeStep',
});

export const IconOrdering = createIcon({
  m: IconOrderingSizeM,
  xs: IconOrderingSizeM,
  s: IconOrderingSizeM,
  name: 'IconOrdering',
});
